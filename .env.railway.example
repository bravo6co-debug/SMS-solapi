# Railway 배포용 환경변수 템플릿
# Railway 대시보드 → Variables 탭에서 이 값들을 설정하세요

# ============================================
# 필수 환경변수 (반드시 설정!)
# ============================================

# SOLAPI API 인증 정보 (https://console.solapi.com/)
SOLAPI_API_KEY=your_actual_api_key_here
SOLAPI_API_SECRET=your_actual_api_secret_here
SOLAPI_SENDER_PHONE=01012345678

# 세션 보안 키 (32자 이상 랜덤 문자열 생성 필수!)
# 생성 방법: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-change-this-in-production

# ============================================
# 자동 설정되는 환경변수 (설정 불필요)
# ============================================

# DATABASE_URL - Railway PostgreSQL 서비스에서 자동 주입
# PORT - Railway가 자동으로 할당

# ============================================
# 선택적 환경변수
# ============================================

# 세션 만료 시간 (시간 단위, 기본값: 24)
# SESSION_EXPIRE_HOURS=24

# 템플릿 최대 개수 (기본값: 10)
# MAX_TEMPLATES=10

# ============================================
# 프로덕션 보안 설정
# ============================================

# HTTPS 전용 쿠키 활성화 (Railway는 기본 HTTPS 제공)
# main.py에서 https_only=True로 변경 권장
